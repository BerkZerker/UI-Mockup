import React from "react";
import ReactDOM from "react-dom/client";
import MicroGoals from "./ui_mockup.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MicroGoals />
  </React.StrictMode>,
);
